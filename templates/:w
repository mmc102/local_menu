<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurants</title>
    <link href="/static/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-4">
    <h1 class="text-3xl font-bold mb-4">Restaurant List</h1>

<div class="mb-4">
    <!-- Toggle Button (Only visible on mobile) -->
    <button 
        id="toggle-categories" 
        type="button" 
        class="mb-2 px-4 py-2 bg-gray-800 text-white rounded hover:bg-gray-700 focus:outline-none block sm:hidden"
        onclick="toggleCategories()"
    >
        Show Filters
    </button>

    <!-- Collapsible Category Tags (Only visible on mobile) -->
    <div id="categories-container" class="flex flex-wrap gap-2 max-w-full hidden sm:hidden">
        {% for category in categories %}
        {% if category.name in selected_categories %}
        <a href="{% if category.name in selected_categories %}/?{% else %}/?categories={{ category.name }}{% endif %}"
           class="px-4 py-2 bg-blue-800 text-white rounded hover:bg-blue-700">
            {{ category.name }}
        </a>
        {% else %}
        <a href="{% if category.name in selected_categories %}/?{% else %}/?categories={{ category.name }}{% endif %}"
           class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-700">
            {{ category.name }}
        </a>
        {% endif %}
        {% endfor %}
    </div>

    <!-- Always-visible Categories (Only visible on desktop) -->
    <div class="hidden sm:flex sm:flex-wrap sm:gap-2 sm:mb-4 sm:max-w-full">
        {% for category in categories %}
        <a href="{% if category.name in selected_categories %}/?{% else %}/?categories={{ category.name }}{% endif %}"
           class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-700">
            {{ category.name }}
        </a>
        {% endfor %}
    </div>
</div>


    <table class="min-w-full bg-white border border-gray-200">
        <thead>
            <tr class="bg-gray-200">
                <th class="px-4 py-2 border">Name</th>
                <th class="px-4 py-2 border hidden sm:table-cell">Location</th>
                <th class="px-4 py-2 border hidden sm:table-cell">Categories</th>
                <th class="px-4 py-2 border">Link</th>
            </tr>
        </thead>
        <tbody>
            {% for restaurant in restaurants %}
            <tr class="hover:bg-gray-100">
                <td class="px-4 py-2 border">{{ restaurant.name }}</td>
                <td class="px-4 py-2 border  hidden sm:table-cell">{{ restaurant.location }}</td>
                <td class="px-4 py-2 border hidden sm:table-cell">
                    {% for category in restaurant.categories %}
                    <span class="inline-block px-2 py-1 bg-gray-300 text-sm rounded">{{ category.name }}</span>
                    {% endfor %}
                </td>
                <td class="px-4 py-2 border">
                    <a href="/restaurants/{{ restaurant.id }}/menu" target="_blank" class="text-blue-500 hover:underline">
                        Visit
                    </a>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
<div class="relative">
    <form method="POST" action="/suggestion" 
        class="max-w-md bg-white p-6 rounded-lg shadow-md fixed bottom-0 left-1/2 transform -translate-x-1/2 sm:left-0 sm:transform-none sm:mb-4 sm:ml-4">
        <h1 class="text-xl font-bold mb-4 text-gray-800">See an issue?</h1>
        <div class="mb-4">
            <input 
                type="textarea" 
                id="suggestion" 
                name="suggestion" 
                placeholder="Suggest a change" 
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500"
            >
        </div>
        <button 
            type="submit" 
            class="w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded-md shadow focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
        >
            Submit
        </button>
    </form>
</div>


    <script>
     function toggleCategories() {
         const container = document.getElementById('categories-container');
         const button = document.getElementById('toggle-categories');

         if (container.classList.contains('hidden')) {
             container.classList.remove('hidden');
             button.textContent = 'Hide Categories';
         } else {
             container.classList.add('hidden');
             button.textContent = 'Show Categories';
         }
     }
    </script>
</body>
</html>



